<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PORTAL LOGIN | H-NOTE</title>
    <style>
        :root { 
            --gold: #f0d78c; 
            --black: #000000;
            --white: #ffffff;
            --dark-grey: #1a1a1a;
        }

        body { 
            margin: 0; background: var(--black); color: var(--white); 
            font-family: 'Times New Roman', serif;
            display: flex; justify-content: center; align-items: center;
            height: 100vh; overflow: hidden;
        }

        /* Background Glow */
        .glow {
            position: absolute; width: 300px; height: 300px;
            background: radial-gradient(circle, rgba(240,215,140,0.1) 0%, transparent 70%);
            z-index: 0;
        }

        .login-card {
            background: var(--dark-grey);
            padding: 50px;
            border: 1px solid var(--gold);
            box-shadow: 0 0 30px rgba(240, 215, 140, 0.1);
            text-align: center;
            z-index: 1;
            width: 100%;
            max-width: 400px;
        }

        h2 { 
            color: var(--gold); 
            letter-spacing: 5px; 
            text-transform: uppercase; 
            margin-bottom: 30px;
            font-size: 1.5rem;
        }

        .input-group { margin-bottom: 20px; text-align: left; }

        label { 
            display: block; 
            font-family: 'Courier New', monospace; 
            font-size: 0.8rem; 
            color: var(--gold); 
            margin-bottom: 5px; 
        }

        input {
            width: 100%;
            padding: 12px;
            background: #000;
            border: 1px solid #444;
            color: var(--white);
            font-family: 'Courier New', monospace;
            box-sizing: border-box;
        }

        input:focus { border-color: var(--gold); outline: none; }

        .login-btn {
            width: 100%;
            padding: 15px;
            background: var(--gold);
            color: var(--black);
            border: none;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 20px;
        }

        .login-btn:hover { background: #fff; box-shadow: 0 0 15px var(--gold); }

        .back-link {
            display: block; margin-top: 25px;
            text-decoration: none; color: #666;
            font-size: 0.8rem; font-family: 'Courier New', monospace;
        }
        .back-link:hover { color: var(--gold); }
    </style>
</head>
<body>

    <div class="glow"></div>

    <div class="login-card">
        <h2>ACCESS PORTAL</h2>
        <form action="#">
            <div class="input-group">
                <label>IDENTIFICATION / EMAIL</label>
                <input type="email" placeholder="student@hnote.com" required>
            </div>
            <div class="input-group">
                <label>PASSKEY</label>
                <input type="password" placeholder="••••••••" required>
            </div>
            <button type="submit" class="login-btn">ENTER GATES</button>
        </form>
        <a href="index.html" class="back-link">← RETURN TO MAIN HALL</a>
    </div>

</body>
</html>
